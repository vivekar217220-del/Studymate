Project setup + Vite + React + Tailwind :- 

Bootstrap Vite+React project, install and configure Tailwind CSS with PostCSS, verify hot-reload works.

FILES :- 

vite.config.js tailwind.config.js postcss.config.js src/index.css src/main.jsx

GIT COMMITS :- 

git commit -m "feat: init vite react project"
git commit -m "feat: configure tailwind css and postcss"

CODE :- 

// vite.config.js :- 
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
export default defineConfig({ plugins: [react()] })
// tailwind.config.js
export default {
content: ['./index.html','./src/**/*.{js,jsx}'],
theme: { extend: {} },
plugins: [],
}

// src/index.css :- 
@tailwind base;
@tailwind components;
@tailwind utilities;

// src/main.jsx :- 
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './aws/config.js'
import './index.css'
ReactDOM.createRoot(document.getElementById('root')).render(
<React.StrictMode>
